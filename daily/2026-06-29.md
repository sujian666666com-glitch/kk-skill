# 2026-06-29 每日 热门 Skill 抓取

## 本次收录

| Skill | installsAllTime | 风险等级 | 结论 | 说明 |
|---|---:|---|---|---|
| searxng | 695 | 🟡 MEDIUM | 收录 | 功能边界清晰，主要是向用户配置的 SearXNG 实例发起检索；已知风险是 `verify=False` 适合本地/自签场景，不适合默认信任远程 HTTPS |
| clawdex | 273 | 🟡 MEDIUM | 收录 | 文档型安全检查 skill，核心仅是调用 `https://clawdex.koi.security/api/skill/<slug>` 查询 verdict，外部依赖明确 |

## 明确排除

| Skill | 原因 |
|---|---|
| imsg | 命中排除名单（消息/隐私面过大） |
| 1password | 命中排除名单（凭证管理类） |
| camsnap | 命中排除名单（摄像头/视频采集） |
| self-improving-agent | 命中排除名单（自我修改/长期自演化） |
| proactive-agent | 命中排除名单（持续自主执行） |
| reflect-learn | 自我反思并提议修改 agent / skill / hooks，属于自我改写类高风险能力 |
| cron-mastery | 说明中明确鼓励 `sessionTarget: "main"`、`systemEvent` 和定时唤醒主代理，权限边界过宽 |
| clawdefender | 目的是防护，但实现质量不足：URL allowlist 采用子串匹配、`--install` 先安装后扫描、文档与脚本能力存在偏差，不宜作为精选安全基线收录 |
| memory-setup | 持久记忆与配置改写能力较强，超出本仓库默认谨慎边界 |
| elite-longterm-memory | 持久记忆/同步/上下文长期保留能力较强，默认高风险 |
| stock-analysis | 外部数据面和自动化分析面较大，且不属于本轮优先收录方向 |
| food-order | 涉及真实下单与外部执行，命中高风险交易类边界 |

## 统计

- 仓库已有：223 个
- 趋势池检查：100 个
- 新候选（仓库未收录）：46 个
- 深度复核候选：5 个
- 通过基础 vetting：2 个
- 明确排除：12 个
- 实际安装：2 个

## 安装列表

- searxng
- clawdex

## 说明

- 数据源：`clawhub explore --sort trending --limit 100 --json`
- 安全检验：排除名单 + `clawhub inspect` 文件级抽样 + 红旗模式扫描
- 关键核验点：
  - `searxng` 为真实脚本型 skill，主要文件是 `scripts/searxng.py`；未见凭证抓取、持久化、自启动或 destructive 行为
  - `clawdex` 为轻量文档型 skill，文件面极小；风险主要来自外部 API 依赖而非本地执行面
  - `clawdefender` 虽定位安全工具，但扫描结果与源码都显示能力/实现存在偏差，本轮不作为 curated 基线收录
- 高风险（凭证/隐私/自动执行/自我修改）热门项默认不收录
