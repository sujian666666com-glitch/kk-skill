# <项目名>

<一句话：这是什么、给谁用。两行以内。>

<!-- ============================================================
     使用说明（块级 HTML 注释在 Claude Code 中注入前会被剥离，
     不消耗上下文；其他工具会计入预算，跨工具团队请酌情精简）

     核心纪律：你写下的每一条都会被真的执行——包括在不该执行的任务上。
     只写你希望每次都被拉动的杠杆。

     每加一条之前先定层。完整流程见 agent-constraints 技能的 SKILL.md，
     三条最常用的判据：
       · agent 读代码能自己知道 ......... 删
       · 能用 linter / hook / 权限规则表达 ... 写成那个，不要写在这里
       · 需要反复应用（每个函数/每次提交）... 写在这里，但必须同时配 hook 兜底
     只写你希望每次都被拉动的杠杆。

     初次使用：删掉所有你还没遇到过对应失败的条目。宁可从三行开始。
     ============================================================ -->

<!-- 不要在此处添加目录结构、技术栈概览或架构说明。
     实证结论：仓库概览不产生收益，且与代码构成双事实源。
     唯一例外是本仓库几乎没有 README / docs 时。 -->

## 命令

- 安装：`<cmd>`
- 测试（全量）：`<cmd>`    # <耗时；若慢，注明"优先跑单测">
- 测试（单个）：`<cmd>`
- 检查：`<cmd>`
- 构建：`<cmd>`

<!-- 只写猜不到的。`npm test`、`cargo build` 这类标准命令删掉。 -->

## 完成标准

改动完成前依次通过，并贴出实际输出，不要只声称通过：

1. `<lint cmd>` → 退出码 0
2. `<typecheck cmd>` → 退出码 0
3. `<test cmd>` → 全绿

<!-- 这一节是全文最值钱的部分：它把"做完了"从感觉变成可判定的信号。
     注意这是建议层，不是强制层。真正不能失败的，配 Stop hook。 -->

## 硬约束

- Never <具体到能被人一眼判定是否违反>
- Never 为了让测试通过而放宽断言、添加 skip 或修改期望值。
- 被阻塞时停下来说明卡在哪，不要绕过，不要降级实现。

<!-- Never 只留真正的红线。写在这里的规则在需要反复应用时，
     约 65% 的运行会至少违反一次，首次失守通常在第 4 次应用附近，
     改存量代码的场景下单次命中率甚至只有四成多。
     所以：红线必须同时配 hook 或 permissions.deny，文件层只是提醒。 -->

## 非功能性边界

- <安全：例如 用户输入必须经 `validators/` 校验后才进 SQL 层>
- <性能：例如 列表接口必须分页，默认上限 100>

<!-- 只有约 15% 的项目写这一节，但它恰恰最不可能从代码里推断出来。
     不适用就整节删掉，不要留空标题。 -->

## 项目特有约定

- <与工具默认值不同的约定：例如 包管理用 `uv`，不要用 pip 或 poetry>
- <保留性约束：例如 `src/auth/` 是自定义中间件，不要重构为标准实现（见 ADR-004）>
- <危险区：例如 `legacy/` 已废弃但生产代码仍在 import，不要删>

<!-- ============================================================
     规则台账 —— 每加一条规则，在这里记下触发它的那次失败。
     维护时据此判断该不该删：对应的失败已被 CI 覆盖了吗？还会发生吗？

     YYYY-MM-DD  <规则>  ← <agent 做了什么、造成什么后果>

     每季度过一遍：命令还跑得通吗？规则已被 CI 覆盖了吗（是则删）？
     描述与现实还相符吗？
     ============================================================ -->
